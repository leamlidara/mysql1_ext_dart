name: mysql1_ext
version: 0.0.4
description: A library for connecting to and querying MySQL & MariaDB databases.
homepage: https://github.com/leamlidara/mysql1_ext_dart

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  collection: ^1.18.0
  crypto: ^3.0.3
  logging: ^1.2.0
  pool: ^1.5.1

dev_dependencies:
  mocktail: ^1.0.3
  options_file:
    git:
      url: https://github.com/rxlabz/optionsfile
      ref: ca741d4
  test: ^1.25.2
  very_good_analysis: ^5.1.0
